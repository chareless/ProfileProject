@model List<User>

@{
    ViewBag.Title = "Profile Project";
    ViewBag.BreadcrumbItemActive = "Arama Sonuçları";
    var data = ViewBag.Text;
}
@Html.Partial("~/Views/Shared/_breadcrumb.cshtml")
 
<div>
    <div class="text-center">
        <h3>Arama Sonuçları: "@data"</h3>
    </div>
    <div class="col-lg-12">
        @if(Model != null && Model?.Count > 0)
        {
            @foreach (var item in Model)
            {
                <h4><a asp-action="Index" asp-controller="Profile" asp-route-id="@item.Id">@item.NameSurname</a></h4>
            }
        }
        else
        {
            <h4>Sonuç Bulunamadı.</h4>
        }
        
    </div>
</div>

@section scripts {
    <!-- [Page Specific JS] start -->
    <script src="../assets/js/plugins/apexcharts.min.js"></script>
    <script src="~/js/utils.js"></script>

    <script src="../assets/js/pages/dashboard-default.js"></script>
}